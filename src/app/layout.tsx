import React from 'react'

export const metadata = {
  title: 'CircuitBay Admin',
  description: 'CircuitBay Inventory Management',
}

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return children
}
